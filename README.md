# Profile_Intelligence
A RESTful API that enriches names using three external APIs (Genderize, Agify, Nationalize), stores the processed data in a database, and provides full CRUD operations with idempotency.
